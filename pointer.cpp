#include <iostream>

using namespace std;
int main()
{
    int a,b,sum,diff;
    cin >> a;
    cin >> b;
    sum = a+b;
    cout << sum << endl;
    diff = abs(a-b);
    cout << diff << endl;
    return 0;
}
